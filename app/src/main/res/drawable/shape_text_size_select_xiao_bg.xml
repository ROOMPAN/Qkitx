<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <size
        android:width="50dp"
        android:height="20dp" />
    <!--//填充色-->
    <solid android:color="@color/login_bg" />
    <!-- 设置描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/login_bg"></stroke>
    <corners
        android:bottomLeftRadius="5dp"
        android:radius="0dp"
        android:topLeftRadius="5dp" />
</shape>