<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置固定填充色 -->
    <solid android:color="@color/white" />
    <!-- 设置描边 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/status_color_grey"></stroke>
    <size
        android:width="100dp"
        android:height="50dp" />

    <corners android:radius="50dp" />

</shape>